# Port override (overrides config.json "port", default: 8888)
PORT=8888

# AI provider API key (used to call the LLM model for AI features)
DEEPSEEK_API_KEY=
# AI_API_KEY=
# OPENAI_API_KEY=

# Optional bearer token required on incoming requests to /ai/* endpoints
AI_API_TOKEN=
